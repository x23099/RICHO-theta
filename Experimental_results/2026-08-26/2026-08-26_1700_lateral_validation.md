# 2026-08-26 17:00 左右条件の追加実機確認

## 結論

`blue_ground_contact_max_aspect_ratio=1.5`と
`raw_ground_distance + normalized_area_min=2000`を用いた左右の追加録画は、
事前に残っていた静的条件を満たし、`PASS`とする。

- 左右ともライブ検出・観測採用・追跡・ODOMが100%。
- 保存後raw動画の独立再処理でも左右とも全フレーム検出。
- `raw_ground_distance`ゲート再生は左右2/2 PASS。
- 実効FPSは左30.014、右30.003で、いずれも30 fpsの±1%以内。
- 有効処理時間p95は左27.05 ms、右26.03 msで、33.33 ms予算内に余裕がある。

これにより、16:30録画で未確認だった`x=±0.3 m, z=0.9 m`が埋まり、
横長輪郭除外の今回の静的実装確認は完了した。本番設定`src/bird_eye_config.json`は
まだ変更せず、動的走行・TTCとAI有効時の負荷は別の検証範囲とする。

## 1. 入力と完全性

| 項目 | 値 |
|---|---|
| アーカイブ | `/home/robo25/Downloads/recoding/202608261700.tar.xz` |
| サイズ | 87,756,252 bytes |
| SHA-256 | `839ae962a0002ad493b3928a597ef42264cca106b079ada9e591cb059e5acb19` |
| `xz -t` | PASS |
| 試行数 | 2 |
| metadata/CSV/raw/BEV/detection | 2/2/2/2/2 |

全試行で次を確認した。

- metadataは`odom_topic=/odom`、要求30 fps、MJPG 1280x720。
- `raw_ground_distance + normalized_area_min=2000`、横長比上限1.5。
- CSVはframe 1からの連番で欠番なし。
- CSV行数とraw/BEV/detection動画のフレーム数が一致。
- 9つの処理時間列は全行で有限かつ0以上。

## 2. ライブ結果

| 条件 | 時間 | frame | 実効FPS | 検出 | 採用 | 追跡 | ODOM | 警告 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 左`x=-0.3, z=0.9 m` | 14.26 s | 429 | 30.014 | 100% | 100% | 100% | 100% | 0 |
| 右`x=+0.3, z=0.9 m` | 14.10 s | 424 | 30.003 | 100% | 100% | 100% | 100% | 0 |

位置中央値は左`x=-0.2689, z=0.8739 m`、右`x=+0.2330, z=0.9082 m`だった。
名目配置との差は左が横3.1 cm・奥行2.6 cm、右が横6.7 cm・奥行0.8 cm。
配置自体の実測値ではなくラベル上の名目値との比較であるため、今回は検出・ゲートの
安定性評価に用い、校正係数の再調整根拠にはしない。

ライブの最大NISは左0.747、右0.081、最大`abs(relative_vz)`は左0.0390 m/s、
右0.0105 m/sで、静止物体に対して追跡は安定していた。

## 3. 左右差とゲート再生

ライブの正規化面積中央値は左2741.7、右2879.9で、左右比は0.952だった。
閾値2000に対する余裕は左1.37倍、右1.44倍で、面積棄却は両方0件。

保存後のraw動画を現設定で独立再処理した結果、左429/429、右424/424を検出した。
検出輪郭の最大横長比は左0.729、右0.676で、真の青箱は上限1.5から十分離れている。

| 正規化距離 | 左 | 右 | 総合 |
|---|---:|---:|---:|
| `forward_z` | FAIL（90.21%採用） | PASS（99.76%） | 1/2 PASS |
| calibrated ground distance | FAIL（95.80%採用） | PASS（99.76%） | 1/2 PASS |
| `raw_ground_distance` | PASS（99.77%採用） | PASS（99.76%） | 2/2 PASS |

旧候補2方式だけ左で面積棄却が発生した一方、現在の`raw_ground_distance`は左右を通した。
今回の独立データも、現在の正規化距離選択を支持する。

## 4. 処理時間

`processing_total_before_csv_ms - processing_capture_read_ms`を有効処理時間とした。

| 条件 | 有効処理p50 | 有効処理p95 | 33.33 ms超 | camera read | BEV | blue | AI | display | 3動画write |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 左 | 22.12 | 27.05 | 0.93% | 8.00 | 6.74 | 2.51 | 0.005 | 1.79 | 9.30 |
| 右 | 22.53 | 26.03 | 0.47% | 7.39 | 6.82 | 2.60 | 0.005 | 1.84 | 9.52 |

単位はms。両方とも30 fpsを維持し、非同期writerなどの追加最適化は現時点で不要。
右で最大69.3 msの単発遅延が1件あったが、継続的な低下はなく、全体実効FPSへの影響はない。
metadata上は`enable_ai=1`だが、当日の起動ログどおりAI依存関係は未導入で、AI有効時の負荷は未評価。

## 5. 判定と残る範囲

### PASS

16:30録画の箱なし・別床・中央・遮蔽と今回の左右を合わせ、横長輪郭除外について
予定していた静的実機条件をすべて確認した。箱なし誤検出0、真の青箱保持、遮蔽時の失効・再捕捉、
左右観測、処理時間計測がすべて合格している。

### 別途必要な検証

- AIを使用するなら、PyTorch/Ultralytics導入後の処理時間再計測。
- 動的走行での相対速度、衝突経路、TTC、警告状態遷移。
- 本番設定へ反映する場合の独立holdoutと設定昇格判断。

## 成果物

- `Experimental_results/2026-08-26_1700_archive_inventory.csv`
- `Experimental_results/2026-08-26_1700_live_summary.csv`
- `Experimental_results/2026-08-26_1700_lateral_gate_diagnosis.csv`
- `Experimental_results/2026-08-26_1700_processing_timing.csv`
- `Experimental_results/2026-08-26_1700_observation_replay.csv`
- `Experimental_results/2026-08-26_1700_gate_regression.csv`
