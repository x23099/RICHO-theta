# Windowsバッテリーモニター開発 引き継ぎ資料

## 1. 目的

自宅のWindows PCで、次の無線機器のバッテリー情報を一つの常駐画面に表示する。

- Logicool製ワイヤレスマウス
- Logicool以外のワイヤレスイヤホン

目標とするUIは、Windowsのタスクバー付近に常駐し、通常は残量だけを小さく表示し、
クリック時に機器名、接続状態、残量、最終更新時刻などを展開表示する形式とする。

この文書はUbuntu PCからWindows PCへ開発作業を引き継ぐためのものである。
Windows固有のG HUB、Bluetooth、タスクトレイおよび実機を使用する処理は、Windows PC上で
開発・検証する。

## 2. 現時点で確認できている事実

参考にしているX投稿の作者から、次の回答を得ている。

- バッテリー情報は、G HUBがローカルで使用しているWebSocket経由で取得している。
- `localhost`上のG HUBと通信し、認識済みデバイス一覧とバッテリー情報を取得している。
- 公式公開APIではないため、G HUBの更新で仕様が変わる可能性がある。
- 現状はG HUBを情報源としているため、Logicool製品以外には対応していない。
- 作者から、作成済みツールを後日共有してもらう予定である。

投稿URL:

- <https://x.com/urovalo/status/2092007220276044107?s=46>

## 3. まだ確定していない事項

共有物を受け取るまで、以下は未確定として扱う。推測で依存関係やプロジェクトを作らない。

- ソースコードと実行ファイルのどちらが共有されるか
- 開発言語とUIフレームワーク
- ビルド方法と必要なランタイム
- 対応済みLogicool製品
- G HUB WebSocketのポート、パス、要求・応答形式
- 個人利用、改変、再配布に関する条件
- Bluetoothイヤホン機能を追加できる構造か

## 4. 共有物を受領した直後の手順

### 4.1 原本を保全する

1. 受領ファイルを変更せず、原本用フォルダーへ保存する。
2. ファイル名、受領日時、配布元URLまたは会話を記録する。
3. ZIPなどの場合は、展開前ファイルも残す。
4. SHA-256を記録する。

PowerShell例:

```powershell
Get-FileHash -Algorithm SHA256 .\received-file.zip
```

### 4.2 実行前に内容を確認する

- ソースコードがある場合は、まずコード、依存関係、ビルドスクリプトを確認する。
- 実行ファイルは、Windows Defenderでスキャンしてから扱う。
- 管理者権限を要求するか確認する。通常、この用途に管理者権限は不要である。
- 外部サーバーへの通信、資格情報の取得、任意コマンド実行がないことを確認する。
- WebSocketの接続先がループバックアドレスに限定されていることを確認する。
- ライセンスまたは作者が示した利用条件を記録する。

### 4.3 作者へ追加確認が必要になる条件

次の場合だけ、実物を確認した後に作者へ質問する。

- ビルド手順が不足している。
- 改変可否が明記されていない。
- G HUBの対応バージョンや確認済み機種が分からない。
- WebSocketの仕様がコードから特定できない。
- 実行時に必要な設定やファイルが不足している。

## 5. Windows PCで最初に記録する情報

以下をこの文書または別の実験メモへ追記する。

| 項目 | 記録値 |
|---|---|
| Windowsエディション・バージョン | 未記入 |
| OSビルド | 未記入 |
| Logicoolマウス型番 | 未記入 |
| マウス接続方式 | LIGHTSPEED / Bluetooth / 未確認 |
| G HUBバージョン | 未記入 |
| G HUB上でマウス残量が見えるか | 未確認 |
| イヤホン型番 | 未記入 |
| イヤホン接続方式 | Bluetooth LE / Classic / 未確認 |
| Windows設定でイヤホン残量が見えるか | 未確認 |
| 必要な表示 | 合計 / 左右 / ケース / 未決定 |

Windowsの「設定」→「Bluetoothとデバイス」でイヤホンの残量が表示されるかを確認する。
合計残量が表示されても、左右やケースの残量を個別取得できるとは限らない。

## 6. 開発環境の決め方

共有されたプロジェクトの構成を優先し、受領前に別フレームワークへ移植しない。

### C#の場合

- Windows上のVisual StudioまたはVS Codeを使用する。
- プロジェクトが指定する.NET SDKを導入する。
- WPFまたはWinFormsなら既存構成を維持する。
- WinUI 3の場合はWindows App SDKの指定バージョンを確認する。

### Pythonの場合

- Windows用の仮想環境をプロジェクト内へ作成する。
- `requirements.txt`または`pyproject.toml`の指定を優先する。
- Windows Bluetooth APIやタスクトレイを実機上で検証する。

### ElectronまたはTauriの場合

- ロックファイルに記録されたパッケージマネージャーとバージョンを維持する。
- Windows用ビルドをWindows上で作成する。

WSLはコードの閲覧やGit操作には使用できるが、Windowsのタスクトレイ、Bluetooth API、
G HUBおよび実機統合の主実行環境にはしない。

## 7. 推奨アーキテクチャ

既存ツールを改変できる場合は、表示処理へ直接機種固有コードを追加せず、情報取得元を
Providerとして分離する。

```text
LogitechGHubProvider ───────┐
                            │
BluetoothBatteryProvider ───┼─ DeviceBatteryState ─ Cache ─ Tray/Flyout UI
                            │
ModelSpecificProvider ──────┘
```

共通状態の例:

```text
DeviceBatteryState
  id
  displayName
  percentage          nullを許可
  isCharging          nullを許可
  isConnected
  estimatedHours      nullを許可
  source
  updatedAt
  isStale
```

取得できない値を`0%`として表示しない。`取得不可`、`--`または古い値と最終更新時刻を表示する。

## 8. Logicool製品の取得方針

第一候補は、作者の実装と同じG HUBローカルWebSocketである。

- G HUBが起動している場合だけ接続する。
- 接続先は`localhost`またはループバックアドレスに限定する。
- G HUBの停止・再起動時は、指数バックオフを使って再接続する。
- 要求・応答をUIへ直結させず、内部の共通状態へ変換する。
- 不明なデバイスやフィールドが返ってもアプリ全体を停止させない。
- G HUBバージョンと最後に正常動作したプロトコルをログへ残す。

既存事例では`ws://localhost:9010`が使用されているが、非公開仕様であるため、作者のソースまたは
Windows PC上の実際の通信から確認するまで固定値として採用しない。

参考実装:

- LogiBAT: <https://github.com/someweirdhuman/LogiBAT>
- LGSNotifyBattery: <https://github.com/nixxquality/LGSNotifyBattery>

## 9. Logicool以外のイヤホンの取得方針

次の順序で確認する。

1. Windowsが公開しているデバイスのバッテリープロパティ
2. Bluetooth LE標準Battery Service
3. 機種固有プロトコル

Bluetooth LE標準サービスを使用する場合の識別子は次のとおりである。

- Battery Service: `0x180F`
- Battery Level characteristic: `0x2A19`

公式資料:

- Microsoft Bluetooth GATT Client:
  <https://learn.microsoft.com/windows/apps/develop/devices-sensors/gatt-client>
- `GattCharacteristicUuids.BatteryLevel`:
  <https://learn.microsoft.com/uwp/api/windows.devices.bluetooth.genericattributeprofile.gattcharacteristicuuids.batterylevel>
- Windows Bluetooth battery guidelines:
  <https://learn.microsoft.com/windows-hardware/design/accessory-guidelines/bluetooth-accessory-guidelines/bluetooth-accessory-guidelines-battery-profiles-services>

イヤホンが標準サービスを公開しない場合は、メーカー固有の実装が必要になる。特に左右イヤホンと
ケースの個別残量は、合計残量より取得できない可能性が高い。機種固有対応へ進む前に、型番と
Windows設定画面で見える情報を記録する。

## 10. 更新周期

- G HUBはイベント受信または30～60秒間隔を基本とする。
- Bluetoothは通知を利用できる場合は通知を優先する。
- 通知がない場合も30～60秒間隔を基本とし、毎秒ポーリングしない。
- 切断中は再試行間隔を徐々に長くする。
- 最終更新時刻を保持し、一定時間更新できなければ`isStale`にする。

高頻度のBluetoothまたはHID問い合わせは、機器のスリープや電池消費へ影響する可能性がある。

## 11. UI方針

- Windows標準の通知領域へアイコンを置く。
- 通常表示は小さくし、クリック時だけ詳細を展開する。
- タスクバー内部へコードを注入しない。
- 枠なしウィンドウをタスクバーに隣接させ、見た目だけ統合する。
- 複数ディスプレイ、DPIスケーリング、タスクバー位置変更を考慮する。
- 残量、接続状態、取得元、最終更新時刻を区別して表示する。
- 残り時間は取得元が提供する場合だけ表示し、独自推定なら`推定`と明記する。

## 12. 実装順序

### フェーズ1: 受領版の再現

1. 原本を保全してソースを確認する。
2. 作者指定の環境を構築する。
3. 変更せずビルドする。
4. G HUB起動中にLogicoolマウスが表示されることを確認する。
5. G HUB停止、再起動、デバイスのスリープと復帰を確認する。

### フェーズ2: 内部構造の分離

1. G HUB取得処理を`LogitechGHubProvider`相当へ分離する。
2. UIへ渡す共通データ型を作成する。
3. 通信エラーと未取得値を表現できるようにする。
4. 既存のLogicool表示が回帰していないことを確認する。

### フェーズ3: イヤホン対応

1. Windows設定画面に残量が表示されるか確認する。
2. Windowsデバイスプロパティから取得を試す。
3. BLE機器ならBattery Serviceを列挙して`0x2A19`を読む。
4. 得られた値を共通データ型へ変換する。
5. 左右・ケースが必要な場合だけ機種固有調査へ進む。

### フェーズ4: 常駐運用

1. タスクトレイと詳細表示を整える。
2. 自動再接続と古い値の表示を実装する。
3. Windowsログイン時の自動起動を、ユーザーが選択できる設定として追加する。
4. リリース用ビルドと設定保存方法を文書化する。

## 13. テスト項目

| テスト | 期待結果 |
|---|---|
| G HUB起動済みでアプリ起動 | マウス名と残量が表示される |
| アプリ起動後にG HUBを終了 | アプリは落ちず、切断または更新停止を表示する |
| G HUBを再起動 | 自動的に再接続する |
| マウスをスリープさせる | 不正な`0%`を表示しない |
| マウスを復帰させる | 一定時間内に残量が更新される |
| イヤホンを接続 | 対応機種なら残量を表示する |
| イヤホンをケースへ戻す | 切断または古い値として表示する |
| Bluetoothを無効化 | アプリは落ちず、取得不可を表示する |
| G HUB更新後 | 接続、一覧取得、残量取得を再確認する |
| Windows再ログイン | 設定した場合だけ自動起動する |
| DPI・複数画面変更 | UIが画面外へ出ない |

## 14. MVP完了条件

次をすべて満たした時点を最初の完成とする。

- 対象のLogicoolマウスの残量を取得できる。
- 対象のイヤホンの少なくとも合計残量を、機器が公開している場合に取得できる。
- 両方を同じタスクトレイ／詳細画面へ表示できる。
- G HUB、Bluetoothまたは各デバイスが切断してもアプリが終了しない。
- 値が未取得、切断中、古い状態をUI上で区別できる。
- ポーリングによりデバイスのスリープを常時妨げない。
- ビルド方法、必要なランタイム、対応確認済み機種をREADMEへ記録している。

## 15. 中止・再検討条件

- 作者の利用条件で改変が許可されていない場合は、共有コードを改変しない。
- 実行ファイルしかなく内部仕様も不明な場合は、バイナリ改造を行わない。
- その場合は、既存ツールと別アプリを併用するか、公開情報からクリーンルームで新規実装する。
- イヤホンが残量情報を公開していない場合は、推測値を正式な残量として表示しない。
- G HUB更新で仕様が変わった場合は、旧仕様を継ぎ足さず、プロトコル差分を確認してProviderを更新する。

## 16. Windows側の担当者またはAIへの開始指示

Windows PCへこの文書と受領ファイルを移した後、次の指示から開始する。

> `windows_battery_monitor_handoff.md`を最初から最後まで読み、受領ファイルを実行する前に
> 構成、依存関係、通信先、利用条件を確認してください。既存コードを変更せずにビルドできるかを
> 調査し、Logicoolマウスでの再現確認計画を作成してください。その後、対象イヤホンの型番、
> Windowsが公開するバッテリー情報、Bluetooth Battery Service対応を確認し、Provider分離方式で
> 追加実装してください。未確認の仕様を推測で固定せず、変更前に調査結果を報告してください。

